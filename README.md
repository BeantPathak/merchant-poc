Install prerequisites

Docker + LocalStack

AWS CLI

Python 3.10 + pip

pg8000


localstack start

docker compose down -v

docker compose up -d


Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

.\deploy.ps1

